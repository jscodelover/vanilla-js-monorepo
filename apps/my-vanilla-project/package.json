{
  "name": "my-vanilla-project",
  "version": "1.0.0",
  "main": "index.js",
  "author": "manisha basra",
  "license": "MIT",
  "scripts": {
    "clean": "yarn run rimraf dist",
    "build:css": " yarn run clean && yarn run sass -w -c src:dist --style=compressed"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "sass": "^1.64.0",
    "typescript": "^5.1.6"
  }
}
